<?xml version="1.0" encoding="utf-8"?>
<!-- Màn hình chính giờ CHỈ còn 1 màn "chọn 1 trong 3 app" (xem MainActivity.buildChooser()) -
     mọi thứ khác (WebView riêng của MainActivity, thanh địa chỉ, hình nền, widget, danh sách
     ứng dụng...) đã bị xoá bỏ hoàn toàn theo yêu cầu. rootFrame chỉ còn nền đen thuần, nội dung
     (3 icon) được dựng bằng code trong MainActivity, đè lên trên. -->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/rootFrame"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#000000" />
